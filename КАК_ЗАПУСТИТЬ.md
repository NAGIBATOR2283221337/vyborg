# 🚀 Как запустить систему обработки отчётов

## Быстрый старт

### 1️⃣ Запуск сервера

Дважды кликните на файл:
```
START_SERVER.bat
```

Или запустите вручную:
```cmd
cd C:\Users\User\Desktop\PythonProject\PythonProject
python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000 --reload
```

### 2️⃣ Открытие в браузере

Откройте в браузере:
```
http://127.0.0.1:8000
```

### 3️⃣ ⚠️ ВАЖНО: Очистка кеша браузера

После запуска сервера **ОБЯЗАТЕЛЬНО** очистите кеш:

- **Windows**: `Ctrl + Shift + R` или `Ctrl + F5`
- **Mac**: `Cmd + Shift + R`

Или:
1. Откройте DevTools (`F12`)
2. Кликните правой кнопкой на кнопку обновления
3. Выберите "Очистить кеш и выполнить жёсткую перезагрузку"

---

## 📋 Что вы увидите

После очистки кеша откроется **НОВЫЙ** интерфейс с:

✅ **Одной универсальной формой** (вместо трёх отдельных)
✅ Выпадающим списком для выбора типа отчёта:
   - Российский отчёт
   - Иностранный отчёт  
   - Третий отчёт (заглушка)
✅ Динамическим описанием, меняющимся при выборе типа
✅ Полями загрузки файлов (сетка + отчёт)
✅ Настройками параметров обработки

---

## 🛠️ Устранение проблем

### Проблема: Видите старый интерфейс (две карточки)

**Решение**: Кеш не очищен!
1. Нажмите `Ctrl + Shift + R`
2. Или `F12` → вкладка Network → поставьте галочку "Disable cache" → обновите страницу

### Проблема: ERR_ADDRESS_INVALID при открытии 0.0.0.0:8000

**Решение**: Используйте `127.0.0.1:8000` вместо `0.0.0.0:8000`

### Проблема: Сервер не запускается

**Решение**: 
1. Убедитесь, что Python установлен
2. Установите зависимости: `pip install -r requirements.txt`
3. Проверьте, что порт 8000 свободен

---

## 📝 Как использовать

1. **Выберите тип отчёта** из выпадающего списка
2. **Загрузите файл сетки** (.xlsx)
3. **Загрузите файл отчёта** (.xlsx)
4. **Настройте параметры** (опционально):
   - Максимальное количество показов (по умолчанию: 3)
   - Порог нечёткого поиска (по умолчанию: 0.70 = 70% совпадение)
   - Минимальное пересечение токенов (по умолчанию: 0.50 = 50% общих слов)
   - Удалять несовпадающие строки (по умолчанию: выключено)
5. **Нажмите кнопку "Обработать отчёт"**
6. **Дождитесь загрузки** готового файла

---

## 🔗 Полезные ссылки

- Главная страница: http://127.0.0.1:8000
- API документация: http://127.0.0.1:8000/docs
- Health check: http://127.0.0.1:8000/health

---

## 💡 Подсказки

- Поддерживается **drag & drop** файлов
- Файлы автоматически валидируются (только .xls/.xlsx, макс. 100MB)
- Прогресс-бар показывает статус обработки
- Готовый файл скачивается автоматически

---

**Создано:** 2025-11-13  
**Версия:** 2.0 (Единая форма)

